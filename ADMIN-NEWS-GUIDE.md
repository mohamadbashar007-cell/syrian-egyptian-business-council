# لوحة إدارة الأخبار على الجهاز المحلي

الموقع ولوحة الإدارة يعملان من خادم Node واحد على هذا الجهاز، ولا يحتاجان Vercel أو خدمة تخزين خارجية.

## التشغيل

من داخل مجلد المشروع:

```powershell
npm run serve
```

بعدها افتح:

- الموقع: `http://localhost:3000`
- لوحة الإدارة الخاصة: `http://localhost:3000/council-desk-83`

لتشغيل نسخة سبق بناؤها استخدم `npm start` فقط.

## التخزين

- الأخبار: `storage/news.json`
- الصور: `storage/images/`
- آخر 20 نسخة احتياطية: `storage/backups/`
- رسائل نموذج التواصل: `storage/messages/`

مجلد `storage` وملف `.env.local` مستبعدان من Git، لذلك لا تُرفع الصور أو الأسرار إلى GitHub. يجب نسخ مجلد `storage` دورياً إلى قرص احتياطي.

## الحماية

- لا يوجد رابط للوحة في قوائم الموقع.
- لا تُحفظ كلمة المرور الأصلية؛ يتحقق الخادم منها باستخدام بصمة Scrypt مملّحة في `ADMIN_PASSWORD_HASH`.
- جلسة الدخول `HttpOnly` و`SameSite=Strict` وتنتهي بعد 8 ساعات.
- مسار اللوحة ممنوع من الأرشفة ومن العرض داخل إطار خارجي.
- رفع الصور وتغيير الأخبار لا يعملان دون جلسة إدارة صحيحة.

## الوصول من جهاز آخر

يمكن فتح الموقع من أجهزة الشبكة المحلية باستخدام عنوان IP لهذا الجهاز مع المنفذ 3000، بشرط السماح للمنفذ في جدار حماية Windows. للنشر على الإنترنت يجب استخدام HTTPS وربط اسم النطاق بهذا الخادم عبر Reverse Proxy مثل Caddy أو Nginx.
